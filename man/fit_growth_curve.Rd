% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_growth_curve.R
\name{fit_growth_curve}
\alias{fit_growth_curve}
\title{Fit a Parametric Growth Model}
\usage{
fit_growth_curve(data, model = c("logistic", "gompertz"))
}
\arguments{
\item{data}{Growth curve data for one sample in tidy, wide, or
\code{SummarizedExperiment} format.}

\item{model}{Model type: \code{"logistic"} or \code{"gompertz"}.}
}
\value{
An object of class \code{growkar_fit}. Failed fits return the same class
with \code{converged = FALSE} and diagnostic fields describing the failure.
}
\description{
Fit a logistic or Gompertz growth model to a single sample.
}
\examples{
data(yeast_growth_data)
tidy_growth <- as_tidy_growth_data(yeast_growth_data)
sample_id <- unique(tidy_growth$sample)[1]
fit <- fit_growth_curve(
  dplyr::filter(tidy_growth, sample == sample_id),
  model = "logistic"
)
fit
}
