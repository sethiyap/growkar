% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_doubling_time.R
\name{plot_doubling_time}
\alias{plot_doubling_time}
\title{Plot Doubling Time Summary}
\usage{
plot_doubling_time(
  data,
  comparison_col = NULL,
  compare_to = NULL,
  exclude_groups = NULL,
  select_replicates = NULL,
  average_replicates = FALSE,
  method = c("rolling_window", "defined_interval", "rule_based"),
  error = c("se", "sd"),
  pvalue_method = c("t_test", "wilcox"),
  palette_name = "all_colors",
  custom_colors = NULL,
  ...
)
}
\arguments{
\item{data}{Growth curve data in tidy or wide format.}

\item{comparison_col}{Column used to group replicate-level doubling times.
Typical values are \code{"condition"} or \code{"sample"}.}

\item{compare_to}{Optional reference group used for p-value comparisons.}

\item{exclude_groups}{Optional character vector of groups in \code{comparison_col}
to remove from the plotted summary.}

\item{select_replicates}{Optional character vector of replicate IDs to retain
before computing doubling times.}

\item{average_replicates}{Logical; if \code{TRUE}, compute doubling time from
averaged replicate trajectories and plot one bar per averaged sample.}

\item{method}{Estimation method passed to \code{summarize_growth_metrics()}.}

\item{error}{Statistic used for error bars. One of \code{"se"} or \code{"sd"}.}

\item{pvalue_method}{Statistical test used when \code{compare_to} is supplied.
One of \code{"t_test"} or \code{"wilcox"}.}

\item{palette_name}{Name of the qualitative palette used by
\code{select_palette()}.}

\item{custom_colors}{Optional character vector of colours. When supplied,
these user-defined colours are used instead of the selected palette.}

\item{...}{Additional arguments passed to \code{summarize_growth_metrics()}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plot replicate-based doubling-time summaries as a bar chart with error bars
and optional bracketed p-value annotations.
}
\examples{
data(yeast_growth_data)
plot_doubling_time(
  yeast_growth_data,
  comparison_col = "condition",
  compare_to = "Cg",
  palette_name = "Dark2"
)
}
