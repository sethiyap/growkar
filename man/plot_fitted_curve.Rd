% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_fitted_curve.R
\name{plot_fitted_curve}
\alias{plot_fitted_curve}
\title{Plot a Fitted Growth Curve}
\usage{
plot_fitted_curve(
  fit,
  model = c("logistic", "gompertz"),
  select_replicates = NULL,
  average_replicates = FALSE,
  colour_col = "sample",
  facet_col = NULL,
  palette_name = "all_colors",
  custom_colors = NULL
)
}
\arguments{
\item{fit}{A \code{growkar_fit} object, or growth curve data in tidy or wide
format.}

\item{model}{Model type used when \code{fit} is raw data rather than a
\code{growkar_fit} object.}

\item{select_replicates}{Optional character vector of replicate IDs to retain
before fitting. When \code{NULL}, all replicates are retained.}

\item{average_replicates}{Logical; if \code{TRUE}, average replicate trajectories
before fitting and plotting.}

\item{colour_col}{Name of the column mapped to colour when plotting from raw
data.}

\item{facet_col}{Optional name of a column used for faceting when plotting
from raw data.}

\item{palette_name}{Name of the qualitative palette used by
\code{select_palette()}. Supported values include \code{"all_colors"} (combined
palette), \code{"Accent"} (8), \code{"Dark2"} (8), \code{"Paired"} (12), \code{"Pastel1"} (9),
\code{"Pastel2"} (8), \code{"Set1"} (9), \code{"Set2"} (8), and \code{"Set3"} (12).}

\item{custom_colors}{Optional character vector of colours. When supplied,
these user-defined colours are used instead of the selected palette.}
}
\value{
A \code{ggplot2} object.
}
\description{
Overlay observed points and fitted values from a parametric growth model.
}
\examples{
tidy_growth <- as_tidy_growth_data(yeast_growth_data)
sample_id <- unique(tidy_growth$sample)[1]
fit <- fit_growth_curve(dplyr::filter(tidy_growth, sample == sample_id))
plot_fitted_curve(fit)
}
