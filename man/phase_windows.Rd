% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/se_helpers.R
\name{phase_windows}
\alias{phase_windows}
\title{Compute and Store Exponential-Phase Windows}
\usage{
phase_windows(
  data,
  window_size = 4L,
  min_od = 0.02,
  average_replicates = FALSE,
  select_replicates = NULL,
  store_metadata = TRUE
)
}
\arguments{
\item{data}{Growth data for one sample or multiple samples in tidy, wide, or
\code{SummarizedExperiment} format.}

\item{window_size}{Number of observations in each rolling window.}

\item{min_od}{Minimum OD retained for log-linear fitting.}

\item{average_replicates}{Logical; if \code{TRUE}, average replicate trajectories
before detection when replicate metadata are available.}

\item{select_replicates}{Optional character vector of replicate IDs to retain
before detection. When \code{NULL}, all replicates are retained.}

\item{store_metadata}{Logical; when \code{TRUE} and \code{data} is a
\code{SummarizedExperiment}, store the detected windows and analysis parameters
in \code{metadata()}.}
}
\value{
A tibble for non-\code{SummarizedExperiment} input, or an updated
\code{SummarizedExperiment} when \code{data} is a \code{SummarizedExperiment}.
}
\description{
Detect exponential-phase windows for tidy, wide, or \code{SummarizedExperiment}
input. When \code{data} is a \code{SummarizedExperiment}, the detected windows are
stored in \code{metadata(data)$exponential_phase_windows}, and the updated object
is returned.
}
\examples{
data(yeast_growth_data)
se <- as_summarized_experiment(yeast_growth_data)
se <- phase_windows(se, average_replicates = TRUE)
S4Vectors::metadata(se)$exponential_phase_windows
}
