% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/se_helpers.R
\name{fit_growth_models}
\alias{fit_growth_models}
\title{Fit and Store Growth Models}
\usage{
fit_growth_models(
  data,
  model = c("logistic", "gompertz"),
  store_metadata = TRUE
)
}
\arguments{
\item{data}{Growth curve data in tidy, wide, or \code{SummarizedExperiment}
format.}

\item{model}{Model type: \code{"logistic"} or \code{"gompertz"}.}

\item{store_metadata}{Logical; when \code{TRUE} and \code{data} is a
\code{SummarizedExperiment}, store fitted models and parameter summaries in
\code{metadata()}.}
}
\value{
A tibble with fitted models for non-\code{SummarizedExperiment} input, or
an updated \code{SummarizedExperiment} when \code{data} is a \code{SummarizedExperiment}.
}
\description{
Fit logistic or Gompertz growth models for tidy, wide, or
\code{SummarizedExperiment} input. When \code{data} is a \code{SummarizedExperiment}, fitted
model objects and extracted parameter summaries are stored in \code{metadata()},
and the updated object is returned.
}
\examples{
data(yeast_growth_data)
se <- as_summarized_experiment(yeast_growth_data)
se <- fit_growth_models(se, model = "logistic")
S4Vectors::metadata(se)$growth_model_parameters
}
