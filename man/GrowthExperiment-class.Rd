% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllClasses.R
\docType{class}
\name{GrowthExperiment-class}
\alias{GrowthExperiment-class}
\title{Growth Assay Experiment}
\value{
A \code{GrowthExperiment} object.
}
\description{
\code{GrowthExperiment} is the data container used throughout \code{growkar}. It is an
S4 class that directly extends
\link[SummarizedExperiment:SummarizedExperiment]{SummarizedExperiment::SummarizedExperiment}, adding a validity method that
enforces the canonical growth-assay layout:
}
\details{
\itemize{
\item an assay named \code{od} holding optical density measurements, with timepoints
in rows and samples in columns;
\item a numeric \code{time} column in \code{rowData()};
\item sample annotations in \code{colData()};
\item derived results (growth metrics, exponential-phase windows, model fits) in
\code{metadata()}.
}

Because the class extends \code{SummarizedExperiment}, every method written for
\code{SummarizedExperiment} — including subsetting, \code{assay()}, \code{rowData()},
\code{colData()}, \code{metadata()}, and the tidyomics verbs provided by
\code{tidySummarizedExperiment} — applies unchanged, and
\code{as(x, "SummarizedExperiment")} is always available for handing objects to
other Bioconductor packages.

Construct objects with \code{\link[=GrowthExperiment]{GrowthExperiment()}}, or coerce with
\code{as(x, "GrowthExperiment")} from a \code{data.frame}, \code{tibble}, \code{matrix}, or
existing \code{SummarizedExperiment}.
}
\examples{
data(yeast_growth_data)

ge <- GrowthExperiment(yeast_growth_data)
ge

# Standard coercion in both directions.
se <- as(ge, "SummarizedExperiment")
identical(ge, as(se, "GrowthExperiment"))

# Inherited SummarizedExperiment methods apply unchanged.
SummarizedExperiment::assayNames(ge)
dim(ge[, 1:3])
}
\seealso{
\code{\link[=GrowthExperiment]{GrowthExperiment()}}, \code{\link[=validate_growth_experiment]{validate_growth_experiment()}}
}
