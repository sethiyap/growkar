% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GrowthExperiment.R
\name{GrowthExperiment}
\alias{GrowthExperiment}
\title{Construct a \code{GrowthExperiment}}
\usage{
GrowthExperiment(
  data,
  metrics = NULL,
  sample_col = "sample",
  time_col = "time",
  od_col = "od",
  sample_sep = "_"
)
}
\arguments{
\item{data}{Growth curve data in tidy or wide format, or a
\code{SummarizedExperiment}.}

\item{metrics}{Optional data frame of precomputed sample-level metrics to
store in \code{metadata(x)$growth_metrics}. Metrics computed by \code{growkar}
itself are normally attached with \code{\link[=growth_metrics]{growth_metrics()}} instead.}

\item{sample_col}{Name of the sample column for long-form input.}

\item{time_col}{Name of the time column.}

\item{od_col}{Name of the optical density column.}

\item{sample_sep}{Separator used to infer metadata columns from sample names.}
}
\value{
A \link[=GrowthExperiment-class]{GrowthExperiment} object with one assay named \code{od}.
}
\description{
Build a \link[=GrowthExperiment-class]{GrowthExperiment} object, the canonical \code{growkar} container,
from tidy or wide growth curve data. \code{GrowthExperiment} extends
\link[SummarizedExperiment:SummarizedExperiment]{SummarizedExperiment::SummarizedExperiment}, so the result can be used
directly by any Bioconductor package.
}
\details{
The resulting object stores time points in \code{rowData(x)$time}, sample-level
metadata in \code{colData(x)}, optical density measurements in
\code{assay(x, "od")}, and derived results in \code{metadata(x)}.

Coercion methods are also provided, so \code{as(x, "GrowthExperiment")} works for
a \code{data.frame}, \code{tibble}, \code{matrix}, or existing \code{SummarizedExperiment}, and
\code{as(x, "SummarizedExperiment")} is available in the other direction. Use the
constructor when you need to control column-name resolution; use \code{as()} for
the default behaviour.
}
\examples{
data(yeast_growth_data)

ge <- GrowthExperiment(yeast_growth_data)
ge

# Equivalent, using standard coercion.
as(yeast_growth_data, "GrowthExperiment")
}
\seealso{
\link[=GrowthExperiment-class]{GrowthExperiment}
}
