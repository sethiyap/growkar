% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/growth_experiment.R
\name{validate_growth_experiment}
\alias{validate_growth_experiment}
\title{Validate a Growth \code{SummarizedExperiment}}
\usage{
validate_growth_experiment(x, require_finite = TRUE)
}
\arguments{
\item{x}{A \code{SummarizedExperiment}.}

\item{require_finite}{Logical; if \code{TRUE}, reject non-finite assay values and
time values.}
}
\value{
The validated \code{SummarizedExperiment}.
}
\description{
Check that a \code{SummarizedExperiment} follows the canonical \code{growkar} layout.
}
\details{
The layout itself (an \code{od} assay, a numeric \code{rowData(x)$time} of matching
length) is enforced by the \link[=GrowthExperiment-class]{GrowthExperiment} validity method, which
this function calls rather than re-checking. Only the finiteness
requirement, which the class deliberately does not impose, is checked here.
}
\examples{
data(yeast_growth_data)
se <- GrowthExperiment(yeast_growth_data)
validate_growth_experiment(se)
}
