% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/se_helpers.R
\name{growth_metrics}
\alias{growth_metrics}
\title{Compute and Store Growth Metrics}
\usage{
growth_metrics(
  data,
  method = c("rolling_window", "defined_interval", "rule_based"),
  average_replicates = FALSE,
  select_replicates = NULL,
  comparison_col = NULL,
  compare_to = NULL,
  error = c("se", "sd"),
  pvalue_method = c("t_test", "wilcox"),
  store_metadata = TRUE,
  ...
)
}
\arguments{
\item{data}{Growth curve data in tidy, wide, or \code{SummarizedExperiment}
format.}

\item{method}{Estimation method passed to \code{compute_growth_rate()}.}

\item{average_replicates}{Logical; if \code{TRUE}, average replicate trajectories
before computing metrics. When \code{select_replicates} is \code{NULL}, all available
replicates are averaged.}

\item{select_replicates}{Optional character vector of replicate IDs to retain
before summarizing. When \code{NULL}, all replicates are retained.}

\item{comparison_col}{Optional column used to summarize replicate-level
doubling times and compute group-wise statistics. Typical values are
\code{"condition"} or \code{"sample"}.}

\item{compare_to}{Optional reference group in \code{comparison_col} used for
doubling-time p-value comparisons.}

\item{error}{Statistic used for doubling-time error bars and summary output.
One of \code{"se"} or \code{"sd"}.}

\item{pvalue_method}{Statistical test used when \code{compare_to} is supplied.
One of \code{"t_test"} or \code{"wilcox"}.}

\item{store_metadata}{Logical; when \code{TRUE} and \code{data} is a
\code{SummarizedExperiment}, store the derived metrics and analysis parameters
in \code{metadata()}.}

\item{...}{Additional arguments passed to \code{compute_growth_rate()}.}
}
\value{
A tidy tibble for non-\code{SummarizedExperiment} input, or an updated
\code{SummarizedExperiment} when \code{data} is a \code{SummarizedExperiment}.
}
\description{
Compute growth metrics for tidy, wide, or \code{SummarizedExperiment} input. When
\code{data} is a \code{SummarizedExperiment}, the resulting metrics table is stored in
\code{metadata(data)$growth_metrics}, and the updated object is returned.
}
\examples{
data(yeast_growth_data)
se <- as_summarized_experiment(yeast_growth_data)
se <- growth_metrics(se, method = "rolling_window", average_replicates = TRUE)
S4Vectors::metadata(se)$growth_metrics
}
