% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_growth_rate.R
\name{compute_growth_rate}
\alias{compute_growth_rate}
\title{Compute Growth Rate}
\usage{
compute_growth_rate(
  data,
  method = c("rolling_window", "defined_interval", "rule_based"),
  interval = NULL,
  select_replicates = NULL,
  average_replicates = FALSE,
  window_size = 5,
  min_od = 0.02,
  first_timepoint = NULL
)
}
\arguments{
\item{data}{Growth curve data in tidy or wide format.}

\item{method}{Estimation method. One of \code{"rolling_window"},
\code{"defined_interval"}, or \code{"rule_based"}.}

\item{interval}{Interval definition for \code{defined_interval}. Supply either a
numeric vector of length two or a data frame whose first three columns are
sample, start time, and end time.}

\item{select_replicates}{Optional character vector of replicate IDs to retain
before estimation. When \code{NULL}, all replicates are retained.}

\item{average_replicates}{Logical; if \code{TRUE}, average replicates before
estimation when replicate metadata are available.}

\item{window_size}{Rolling window size for \code{rolling_window}.}

\item{min_od}{Minimum OD retained when fitting log-linear models.}

\item{first_timepoint}{Reference time used by the legacy \code{"rule_based"}
method.}
}
\value{
A tibble with \code{sample}, \code{mu}, \code{start_time}, \code{end_time}, \code{r_squared},
and \code{method}. Here \code{mu} is the estimated specific growth rate. Additional
diagnostic columns describe the number of points used and whether the
estimate required degraded fallback behavior.
}
\description{
Estimate per-sample growth rate from tidy growth data.
}
\details{
The returned \code{mu} column is the specific growth rate, estimated as the slope
of \code{log(od)} versus time over the selected interval or window.

Available methods:
\itemize{
\item \code{rolling_window}: scans rolling windows across the time series and
selects the window with the strongest positive log-linear slope.
\item \code{defined_interval}: fits the growth rate over a user-supplied start
and end time interval.
\item \code{rule_based}: preserves the legacy \code{growkar} approach based on
OD-doubling heuristics to define the exponential phase.
}
}
\examples{
tidy_growth <- as_tidy_growth_data(yeast_growth_data)
sample_id <- unique(tidy_growth$sample)[1]
compute_growth_rate(
  dplyr::filter(tidy_growth, sample == sample_id),
  method = "rolling_window"
)
}
